{
  "name": "backend",
  "version": "1.0.0",
  "main": "app.ts",
  "scripts": {
    "start": "ts-node-dev --respawn --transpile-only src/app.ts",
    "build": "tsc",
    "deploy": "npm run build && zip -r backend.zip dist/* node_modules/* package.json"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@sentry/node": "^8.35.0",
    "aws-sdk": "^2.1691.0",
    "express": "^4.21.1",
    "mongodb": "^6.9.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.7.7",
    "typescript": "^5.6.3"
  }
}
