{
  "name": "cupidonchik",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "start": "concurrently \"npm start --prefix frontend\" \"npm run start --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "test": "npm test --prefix frontend && npm test --prefix backend"
  },
  "devDependencies": {
    "@types/node": "^22.7.8",
    "concurrently": "^6.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
